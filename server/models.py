from sqlalchemy import Column, Integer, String, ForeignKey, DateTime\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom datetime import datetime\n\nBase = declarative_base()\n\nclass Computer(Base):\n    __tablename__ = 'computers'\n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    created_at = Column(DateTime, default=datetime.utcnow)\n\nclass StatusLog(Base):\n    __tablename__ = 'status_logs'\n    id = Column(Integer, primary_key=True)\n    computer_id = Column(Integer, ForeignKey('computers.id'), nullable=False)\n    status = Column(String, nullable=False)\n    timestamp = Column(DateTime, default=datetime.utcnow)\n\n    computer = relationship('Computer', back_populates='status_logs')\n\nComputer.status_logs = relationship('StatusLog', order_by=StatusLog.timestamp, back_populates='computer')\n\nclass CommandLog(Base):\n    __tablename__ = 'command_logs'\n    id = Column(Integer, primary_key=True)\n    computer_id = Column(Integer, ForeignKey('computers.id'), nullable=False)\n    command = Column(String, nullable=False)\n    timestamp = Column(DateTime, default=datetime.utcnow)\n\n    computer = relationship('Computer', back_populates='command_logs')\n\nComputer.command_logs = relationship('CommandLog', order_by=CommandLog.timestamp, back_populates='computer')\n